# Raw Sources

Place immutable source documents here before asking the LLM to ingest them into the generated wiki.

- `sources/` is for articles, notes, transcripts, exported markdown, converted PDFs, and other source documents.
- `assets/` is for images and attachments referenced by source documents.

The LLM should read from this layer but should not alter source files unless explicitly instructed.
